spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Grupo-8-BDD-OO2-2020?serverTimezone=UTC
    username: root
    password: 
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false

